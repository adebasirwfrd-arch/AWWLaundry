name: Build Android APK

on:
  push:
    branches:
      - main

jobs:
  build_android_apk:
    name: Build Android APK (preview)
    type: build
    params:
      platform: android
      profile: preview
